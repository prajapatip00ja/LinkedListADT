#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ctest/bin/ctest" "$@"
  ret=$?
else 
  node  "$basedir/../ctest/bin/ctest" "$@"
  ret=$?
fi
exit $ret
